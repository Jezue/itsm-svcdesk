# Repository guidance

Implement the published svcdesk API without changing the receipted specification or Git history. Run the local checks before proposing delivery. Preserve the named-volume persistence contract and the selected C1, C2, and C3 behavior in `DECISIONS.md`.

The reviewer agent is read-only: it may inspect code and test output, but it must not delete files, publish Git changes, or manipulate containers.
